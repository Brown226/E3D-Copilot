# Phase 3: 增强功能

> ⏱ **耗时**：1 周（第 3 周，D15~D21）  
> 📅 **前置**：[Phase 2](阶段2-工具扩展.md)（46 工具全部就绪）  
> 🎯 **目标**：长期记忆、会话持久化、Skill 系统原型  

---

## 一、范围限定

### ✅ 本阶段做

| 子阶段 | 内容 |
|--------|------|
| **3a** | 上下文压缩 + 会话持久化 |
| **3b** | 长期记忆系统 |
| **3c** | 安全检查 + 批量限制 |
| **3d** | Skill 系统原型 |

### ❌ 本阶段不做

- 不实现多 Agent 协作（v2）
- 不实现多 Provider（v2）
- 不实现 i18n（v2）
- 不实现 PML 语法 LSP（超出范围）

---

## 二、3a：上下文压缩 + 会话持久化（D15~D16）

### 任务

| # | 任务 | 产出 | 参考文档 |
|:-:|------|------|---------|
| 3a.1 | 上下文压缩（LLM 摘要模式 + 固定窗口尾部保留） | `ContextCompactor.cs` | `架构设计.md` §6 |
| 3a.2 | 会话持久化（JSONL 每行一个消息） | `SessionStore.cs` | `记忆系统.md` §3 |
| 3a.3 | 会话索引（日期/摘要/工具使用统计） | `SessionIndex.cs` | `记忆系统.md` §3.2 |
| 3a.4 | 注入最近 3 次会话摘要到 System Prompt | Session 注入 | `记忆系统.md` §3.3 |

### 完成定义

```
□ 超过 100 条消息自动压缩
□ 会话关闭后自动保存 JSONL
□ 新会话注入最近 3 次摘要
□ 会话索引可检索
```

---

## 三、3b：长期记忆系统（D16~D18）

### 任务

| # | 任务 | 产出 | 参考文档 |
|:-:|------|------|---------|
| 3b.1 | SQLite FTS5 存储 + 自动 reconcile | `MemoryStore.cs` | `记忆系统.md` §5、§9 |
| 3b.2 | remember 工具（保存记忆） | `RememberTool.cs` | `记忆系统.md` §5.3 |
| 3b.3 | forget 工具（删除记忆） | `ForgetTool.cs` | `记忆系统.md` §5.3 |
| 3b.4 | memory 工具（搜索/读取/列出） | `MemoryTool.cs` | `记忆系统.md` §5.3 |
| 3b.5 | 用户画像自动学习 | `UserProfile.cs` | `记忆系统.md` §4 |
| 3b.6 | 画像注入 System Prompt | 更新 SystemPrompt.cs | `记忆系统.md` §4.4 |
| 3b.7 | Memory Suggestion（扫描历史→建议记录） | `MemorySuggestion.cs` | `记忆系统.md` §8 |

### 完成定义

```
□ remember/forget/memory 三工具可用
□ SQLite FTS5 BM25 搜索返回正确排序
□ 用户画像自动更新（操作频率追踪）
□ 画像注入 prompt（"用户偏好：..."）
□ 新对话中可通过 memory 工具读取旧记忆
```

---

## 四、3c：安全检查 + 批量限制（D19）

### 任务

| # | 任务 | 产出 | 参考文档 |
|:-:|------|------|---------|
| 3c.1 | Hook 系统（操作前/后检查管道） | `HookService.cs` | 新设计 |
| 3c.2 | 批量操作数量限制 + 确认阈值 | `BatchLimiter.cs` | `工具设计.md` |
| 3c.3 | 操作日志 + 撤销支持 | `OperationLog.cs` | `错误处理.md` §7 |

### 完成定义

```
□ 批量修改 >10 条触发二次确认
□ 操作日志可查询
□ Hook 系统可插入安全检查
```

---

## 五、3d：Skill 系统（D20~D21）

### 任务

| # | 任务 | 产出 | 参考文档 |
|:-:|------|------|---------|
| 3d.1 | SKILL.md 定义 + 加载 | `SkillStore.cs` | 新设计（参考 MiMo-Code） |
| 3d.2 | Skill 注入 System Prompt | 更新 `SystemPrompt.cs` | — |
| 3d.3 | 内置 Skill 模板：管道检查、批量修改 | `skills/` 目录 | — |
| 3d.4 | 工作流识别（重复模式→Skill 建议） | `WorkflowRecognizer.cs` | `记忆系统.md` §8.2 |

### 完成定义

```
□ SKILL.md 可被加载并注入 prompt
□ 至少 2 个内置 Skill 可用
□ 工作流识别可检测重复操作模式
```

---

## 六、依赖关系

```
D15: 3a.1 (压缩) ──→ 3a.2 (持久化) ──→ 3a.3 (索引)
                                       │
D16: 3a.4 (注入) ←─────────────────────┘
       │
       ├──→ 3b.1 (SQLite)
       │
D17: 3b.2-4 (三工具)
       │
D18: 3b.5-7 (画像+建议)
       │
D19: 3c.1-3 (安全+批量+日志)
       │
D20: 3d.1-3 (Skill 加载)
       │
D21: 3d.4 (工作流识别)
```

**并行**：
- 3a.1 + 3b.1 可并行（上下文压缩和 SQLite 表结构无关）
- 3c.1 + 3d.1 可并行

---

## 七、完成定义（Phase 3 整体）

```
□ 46 个工具全部稳定运行
□ 记忆系统可用（remember/forget/memory）
□ 会话持久化（JSONL）
□ 用户画像自动更新
□ Skill 系统原型可用
□ 代码覆盖率 ≥ 75%
□ 全部文档与实现一致
```

