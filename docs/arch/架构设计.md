# E小智 v1.0 — 架构设计

## 一、架构总览

```
┌──────────────────────────────────────────────────────┐
│  ① E3D DockedWindow (WinForms UserControl)          │
│     聊天面板 + 代码预览 + 操作审批                    │
├──────────────────────────────────────────────────────┤
│  ② CopilotController                                │
│     会话管理 + 审批流 + 事件分发                      │
├──────────────────────────────────────────────────────┤
│  ③ AgentLoop                                        │
│     LLM 调用 → Tool 执行 → 结果注入 → 循环          │
├──────────────────────────────────────────────────────┤
│  ④ Tool Registry (6 核心 + 41 专用 = 47 总计)            │
│     AI 只需记忆 6 个核心工具名                         │
│     ┌──────────┬──────────┬──────────┬──────────┐   │
│     │ query    │ modify   │ check    │ calculate│   │
│     ├──────────┼──────────┼──────────┼──────────┤   │
│     │ export   │ exec_pml │          │          │   │
│     └──────────┴──────────┴──────────┴──────────┘   │
│     ↓ 参数路由到 40 个专用工具                        │
│     QueryElementsTool / SetAttributeTool / ...       │
├──────────────────────────────────────────────────────┤
│  ⑤ PML Execution Engine (核心执行层)                 │
│     PmlEngine + PmlGenerator + CsharpBridge          │
│     PML 脚本执行为主，C# API 直调为辅                │
└──────────────────────────────────────────────────────┘
```

## 二、各层职责

### 2.1 E3D DockedWindow (UI 层)

**职责**: 用户交互界面，嵌入 E3D 环境

**核心组件** (详见 [UI设计.md](../design/UI设计.md)):
- `CopilotForm` : `UserControl` — 主窗口，嵌入 E3D DockedWindow
- `ChatListBox` — 消息列表（三级渲染: Hot/Warm/Cold）
- `ToolCardControl` — 工具调用卡片（可折叠 args/output + Diff）
- `PromptShelfControl` — 审批/问答条（键盘驱动 1/2/3/4）
- `InputPanel` — 输入区域（@提及 + /命令 + 图片附件）
- `StatusBarControl` — 状态栏（Token/费用/模式）

**事件驱动更新 UI**:
```csharp
// 统一事件入口（CopilotForm 订阅 CopilotController 的事件流）
CopilotController.EventSink.OnEvent += (evt) =>
{
    this.BeginInvoke(() =>
    {
        switch (evt.Kind)
        {
            case EventKind.Text:           chatList.AppendStreamText(evt.Text); break;
            case EventKind.Reasoning:      chatList.AppendReasoning(evt.Text); break;
            case EventKind.ToolDispatch:   chatList.AddToolCard(evt.Tool); break;
            case EventKind.ToolResult:     chatList.UpdateToolResult(evt.Tool); break;
            case EventKind.ApprovalRequest:approvalDialog.ShowApproval(evt.Approval); break;
            case EventKind.TurnDone:       chatList.FinalizeMessage(); break;
        }
    });
};
```

### 2.2 CopilotController (编排层)

**职责**: transport-agnostic 会话驱动，多前端共享

**核心接口**:
```csharp
public class CopilotController
{
    // 命令接口
    Task SendAsync(string input);
    Task CancelAsync();
    Task ApproveAsync(string approvalId, bool allow, bool persist);
    void SetPlanMode(bool enabled);

    // 状态查询
    bool IsRunning { get; }
    CopilotState State { get; }
    List<ChatMessage> History { get; }
}
```

**内部状态**:
```csharp
private AgentLoop agent;
private IEventSink sink;
private ToolRegistry tools;
private CopilotSession session;  // 会话持久化
private PermissionPolicy policy; // 权限策略
private Dictionary<string, PendingApproval> approvals; // 待审批
```

**Send 流程**:
```
Send(input)
  → Compose(input)  // 注入记忆、技能、上下文
  → agent.RunAsync(ctx, input)
  → 如果是 plan mode: 产出 ApprovalRequest, 等待审批
  → 审批通过: 退出 plan mode, 继续执行
```

### 2.3 AgentLoop (执行层)

**职责**: 核心 Agent 循环，LLM 调用 + 工具执行

**核心循环**:
```csharp
public async Task RunAsync(CancellationToken ct, string input)
{
    session.Add(new Message(Role.User, input));

    for (int step = 0; step < maxSteps; step++)
    {
        // 1. 调用 LLM 流式完成
        var (text, reasoning, calls, usage) = await StreamAsync(ct);

        // 2. 保存 assistant 消息
        session.Add(new Message(Role.Assistant, text, calls));

        // 3. 无工具调用 → 检查最终答案质量
        if (calls.Count == 0)
        {
            if (FinalReadinessCheck()) return;
            continue; // 质量不达标，重试
        }

        // 4. 执行工具调用
        var results = await ExecuteBatchAsync(calls);

        // 5. 保存工具结果
        session.AddToolResults(results);

        // 6. 上下文压缩（可选）
        maybeCompact();
    }
}
```

**流式 Chunk 处理**:
```csharp
private async Task<(string text, string reasoning, List<ToolCall> calls, Usage usage)>
    StreamAsync(CancellationToken ct)
{
    string text = "";
    string reasoning = "";
    var calls = new List<ToolCall>();
    Usage usage = null;

    // net48：通过回调接收流式 Chunk（不支持 IAsyncEnumerable）
    await provider.StreamAsync(request, (chunk) =>
    {
        switch (chunk.Type)
        {
            case ChunkType.Reasoning:
                reasoning += chunk.Text;
                sink.Emit(new CopilotEvent { Kind = EventKind.Reasoning, Text = chunk.Text });
                break;
            case ChunkType.Text:
                text += chunk.Text;
                sink.Emit(new CopilotEvent { Kind = EventKind.Text, Text = chunk.Text });
                break;
            case ChunkType.ToolCall:
                calls.Add(chunk.ToolCall);
                sink.Emit(new CopilotEvent { Kind = EventKind.ToolDispatch, Tool = chunk.ToolCall });
                break;
            case ChunkType.Usage:
                usage = chunk.UsageData;
                break;
        }
    }, ct);

    return (text, reasoning, calls, usage);
}
```

**工具并行分区** (借鉴 Reasonix):
```csharp
private async Task<List<ToolResult>> ExecuteBatchAsync(List<ToolCall> calls)
{
    var results = new List<ToolResult>();

    // 分区: 连续 ReadOnly 并行, Writer 串行
    var batches = PartitionToolCalls(calls);

    foreach (var batch in batches)
    {
        if (batch.All(c => tools.Get(c.Name).IsReadOnly))
        {
            // 并行执行
            var tasks = batch.Select(c => ExecuteOneAsync(c));
            results.AddRange(await Task.WhenAll(tasks));
        }
        else
        {
            // 串行执行
            foreach (var call in batch)
            {
                results.Add(await ExecuteOneAsync(call));
            }
        }
    }

    return results;
}
```

**单个工具执行** (含权限检查):
```csharp
private async Task<ToolResult> ExecuteOneAsync(ToolCall call)
{
    var tool = tools.Get(call.Name);

    // 1. Plan Mode 检查
    if (planMode && !tool.IsReadOnly)
        return ToolResult.Blocked("Plan mode: write operations disabled");

    // 2. 权限检查
    var approval = await policy.CheckAsync(tool, call.Args);
    if (approval == ApprovalResult.NeedApproval)
    {
        var req = new PendingApproval { Tool = tool, Args = call.Args };
        approvals[req.Id] = req;
        sink.Emit(new CopilotEvent { Kind = EventKind.ApprovalRequest, Approval = req });

        // 阻塞等待用户审批
        var result = await req.WaitAsync();
        if (!result.Allow) return ToolResult.Rejected("User rejected");
    }

    // 3. 执行
    try
    {
        var output = await tool.ExecuteAsync(call.Args);
        sink.Emit(new CopilotEvent { Kind = EventKind.ToolResult, Output = output });
        return ToolResult.Success(output);
    }
    catch (Exception ex)
    {
        return ToolResult.Error(ex.Message);
    }
}
```

### 2.4 Tool Registry (工具层)

**核心接口** (借鉴 Reasonix):
```csharp
public interface IE3DTool
{
    string Name { get; }
    string Description { get; }
    string ParameterSchema { get; }  // JSON Schema
    Task<string> ExecuteAsync(string args);
    bool IsReadOnly { get; }
    ToolCategory Category { get; }
}

public enum ToolCategory
{
    Query,     // 元素查询/属性读取（14个）
    Modify,    // 属性修改/批量操作（9个）
    Check,     // 检查/验证（7个）
    Calculate, // 几何计算（4个）
    Export,    // 导入导出（5个）
    System     // 系统工具（2个）
}
```

**工具注册**:
```csharp
public class ToolRegistry
{
    private Dictionary<string, IE3DTool> tools = new Dictionary<string, IE3DTool>();
    private ToolPolicy policy; // per-tool 策略

    public void Register(IE3DTool tool)
    {
        tools[tool.Name] = tool;
        policy.SetDefault(tool.Name, tool.IsReadOnly ? Approval.Auto : Approval.Ask);
    }

    public IE3DTool Get(string name) => tools.TryGetValue(name, out var t) ? t : null;
    public List<ToolSchema> Schemas() => tools.Values.Select(t => new ToolSchema(t)).ToList();
}
```

**Tool Policy** (借鉴 Cline):
```csharp
public class ToolPolicy
{
    private Dictionary<string, PolicyEntry> policies = new Dictionary<string, PolicyEntry>();

    public void Set(string toolName, ApprovalMode mode, bool enabled = true)
    {
        policies[toolName] = new PolicyEntry { Mode = mode, Enabled = enabled };
    }

    public ApprovalMode GetMode(string toolName)
    {
        if (policies.TryGetValue(toolName, out var p))
            return p.Mode;
        return ApprovalMode.Ask; // 默认需审批
    }
}

public enum ApprovalMode
{
    Auto,    // 自动执行
    Ask,     // 需用户确认
    PlanOnly // 仅在 Plan Mode 可用（只读工具）
}
```

**Tool Presets** (借鉴 Cline):
```csharp
public static class ToolPresets
{
    public static readonly ToolPreset Observe = new("observe", new[]
    {
        ("query_elements", ApprovalMode.Auto),
        ("get_attributes", ApprovalMode.Auto),
        ("get_geometry", ApprovalMode.Auto),
        // 所有写操作禁用
    });

    public static readonly ToolPreset Confirm = new("confirm", new[]
    {
        // 只读工具自动, 写工具需确认
    });

    public static readonly ToolPreset Auto = new("auto", new[]
    {
        // 所有工具自动执行（低风险操作）
    });
}
```

### 2.5 PML Execution Engine (执行层)

**职责**: PML 脚本生成与执行，是工具调用的核心执行层

**设计决策**: PML 为主，C# API 为辅

| 层 | 技术 | 适用场景 |
|----|------|---------|
| 工具接口层 | C# `IE3DTool` | 统一注册、权限控制、事件分发 |
| 执行引擎层 | **PML 脚本** | 复杂查询/修改/批量操作（核心） |
| 底层桥接 | `Command.CreateCommand(str).RunInPdms()` | PML 与 C# 的桥梁（`Aveva.Pdms.Utilities.CommandLine`）|
| 简单操作 | C# API 直调 | 单个属性读写、元素存在性检查 |

**PML 黄金范式** (90%+ 工具的核心骨架):
```
1. 构建集合:  var !coll append coll all TYPE with Matchwild(name,'*PATTERN*')
2. 遍历处理:  DO !val values !coll → $!val 导航 → 属性读写
3. 存在验证:  var !flag exist $!name → TRUEA/FALSEA 分支
4. 外部交互:  import 'PMLFileBrowser' 导入 → writefile 导出
5. 异常处理:  handle (code) / handle any → 跳过/记录/提示
```

**PML 几何操作速查**（PML POSITION/DIRECTION 对象，注意成员是 East/North/Up 不是 E/N/U）:
```pml
!pos = !!ce.Position             -- 读取元素位置
!pos.East = 1000                 -- 东坐标（不是 .E）
!pos.North = 2000                -- 北坐标（不是 .N）
!pos.Up = 3000                   -- 高坐标（不是 .U）
!dist = !posA.Distance(!posB)    -- 两点距离
!mid = !posA.Mid(!posB)          -- 中点
!dir = !posA.Direction(!posB)    -- 方向向量
```

**核心组件**:
```csharp
using Aveva.Pdms.Utilities.CommandLine;

// PML 执行引擎（使用真实的 Command 工厂模式）
public class PmlEngine
{
    // 执行 PML 脚本，返回命令行输出
    public string Run(string pmlScript)
    {
        Command cmd = Command.CreateCommand(pmlScript);
        bool ok = cmd.RunInPdms();   // 错误输出到 PDMS 消息窗
        return ok ? cmd.Result : null;
    }

    // 执行 PML 并检查结果（失败抛异常）
    public string RunWithResult(string pmlScript)
    {
        Command cmd = Command.CreateCommand(pmlScript);
        bool ok = cmd.RunInPdms();
        if (!ok)
            throw new InvalidOperationException(cmd.Error.MessageText);
        return cmd.Result;
    }

    public List<string> ParseOutput(string output) { /* ... */ }
}

// PML 代码生成器
public class PmlGenerator
{
    public string GenerateQuery(string type, string pattern, string scope);
    public string GenerateBatchSet(string type, string attr, string value, string filter);
    public string GenerateCheck(string type, string attribute, string rule);
    public string FromNaturalLanguage(string intent);  // AI → PML
}

// C# ↔ PML 桥接（封装 E3D 真实 API）
public class CsharpBridge
{
    // 执行任意 PML 命令
    public string RunPml(string command)
    {
        Command cmd = Command.CreateCommand(command);
        cmd.RunInPdms();
        return cmd.Result;
    }

    // 取当前元素（静态无参）
    public DbElement GetCe()
    {
        return DbElement.GetElement();   // 静态，取 CE
    }

    // 读属性（正确链路：DbAttribute 工厂 + GetAsString）
    public string GetAttribute(DbElement elem, string attrName)
    {
        DbAttribute attr = DbAttribute.GetDbAttribute(attrName);
        return elem.GetAsString(attr);   // 返回字符串值，含单位格式化
    }

    // 写属性（DbAttribute + DbElement/string/int 等强类型重载）
    public void SetAttribute(DbElement elem, string attrName, string value)
    {
        DbAttribute attr = DbAttribute.GetDbAttribute(attrName);
        elem.SetAttribute(attr, value);  // 注意：14 个重载，string 是其中之一
    }
}
```

**工具执行流程**:
```csharp
// QueryElementsTool 的执行逻辑
public class QueryElementsTool : IE3DTool
{
    public async Task<string> ExecuteAsync(string args)
    {
        // 1. 解析 AI 生成的参数
        var filter = ParseFilter(args);  // "TYPE=PIPE AND DIA=100"
        
        // 2. 生成 PML 脚本
        var pml = PmlGenerator.GenerateQuery(
            filter.Type, filter.Pattern, filter.Scope);
        
        // 3. 执行 PML
        var output = await PmlEngine.Run(pml);
        
        // 4. 解析结果返回
        return PmlEngine.ParseOutput(output);
    }
}
```

### 2.6 E3D Runtime (底层)

**职责**: 封装 E3D API 调用，提供 C# 直调能力

```csharp
// 元素查询 — 简单操作可 C# 直调（DbElement.GetElement 静态取 CE，实例方法按属性导航）
// 注意：E3D 没有 Database.GetElements(filter) 这种静态批量查询，复杂查询交给 PML
// 这里演示单个元素的属性读取
public class QueryElementTool : IE3DTool
{
    public async Task<string> ExecuteAsync(string args)
    {
        // 单个元素属性读取（简单场景，如 "当前元素是什么"）
        DbElement ce = DbElement.GetElement();  // 取 CE
        DbAttribute nameAttr = DbAttribute.GetDbAttribute("NAME");
        string name = ce.GetAsString(nameAttr);
        return $"当前元素: {name}";
        // 批量查询 → 走 PML（query_elements 工具）
    }
}

// 属性修改 — 单个元素用 C# 直调，批量操作用 PML
public class ModifyAttributeTool : IE3DTool
{
    public bool IsReadOnly => false; // 需要审批

    public async Task<string> ExecuteAsync(string args)
    {
        var (elementName, attrName, attrValue) = ParseArgs(args);

        // 用 PML 定位元素（E3D 没有直接按名查的 C# API）
        var ce = PmlBridge.NavigateTo(elementName);  // $!elementName → 返回 DbElement

        // 读旧值
        DbAttribute attr = DbAttribute.GetDbAttribute(attrName);
        string oldValue = ce.GetAsString(attr);

        // 写新值
        ce.SetAttribute(attr, attrValue);

        return $"修改 {elementName}.{attrName}: {oldValue} → {attrValue}";
    }
}
```

## 三、Event 系统

**事件类型** (借鉴 Reasonix):
```csharp
public enum EventKind
{
    TurnStarted,        // 轮次开始
    Reasoning,          // AI 思考过程（流式）
    Text,               // AI 回答文本（流式）
    Message,            // 完整消息（流结束）
    ToolDispatch,       // 工具调用开始
    ToolResult,         // 工具执行结果
    ToolProgress,       // 工具执行进度（长时操作）
    Usage,              // Token 用量
    Notice,             // 通知/警告
    ApprovalRequest,    // 审批请求
    TurnDone,           // 轮次结束
    PlanModeChanged,    // Plan Mode 切换
}
```

**Sink 接口**:
```csharp
public interface IEventSink
{
    void Emit(CopilotEvent evt);
}

// WinForms 实现: 跨线程更新 UI
public class WinFormsEventSink : IEventSink
{
    private readonly Control uiControl;
    public void Emit(CopilotEvent evt)
    {
        uiControl.BeginInvoke(() => HandleEvent(evt));
    }
}
```

## 四、LLM Provider

**vLLM + Qwen 适配** (OpenAI 兼容接口，net48 版):

> ⚠️ net48 不支持 `IAsyncEnumerable<T>` / `await foreach`。流式处理改为 `Task` + `StreamReader` 逐行回调。

```csharp
public class VllmProvider : ICopilotProvider
{
    private readonly HttpClient http;
    private readonly string baseUrl = "http://localhost:8000/v1";  // vLLM 默认端口
    private readonly string model = "Qwen3.6-72B";  // 或 Qwen3.5-32B

    // net48 流式方案：传入回调，内部逐行调用
    public async Task StreamAsync(
        CopilotRequest request,
        Action<Chunk> onChunk,
        CancellationToken ct)
    {
        var body = new
        {
            model = request.Model ?? model,
            messages = request.Messages.Select(m => new { role = m.Role, content = m.Content }),
            tools = request.Tools.Select(t => new { /* JSON Schema */ }),
            stream = true
        };

        // vLLM 兼容 OpenAI 格式: /v1/chat/completions
        var response = await http.PostAsJsonAsync($"{baseUrl}/chat/completions", body, ct);
        var stream = await response.Content.ReadAsStreamAsync(ct);

        using (var reader = new StreamReader(stream))
        {
            while (!reader.EndOfStream)
            {
                ct.ThrowIfCancellationRequested();
                var line = await reader.ReadLineAsync();
                if (string.IsNullOrEmpty(line)) continue;

                var json = JsonSerializer.Deserialize<VllmChunk>(line);

                if (json.Choices != null && json.Choices.Length > 0
                    && json.Choices[0].Delta != null)
                {
                    var delta = json.Choices[0].Delta;

                    if (delta.ToolCalls != null)
                    {
                        foreach (var tc in delta.ToolCalls)
                            onChunk(Chunk.ToolCall(tc.Function.Name, tc.Function.Arguments));
                    }
                    else if (delta.Content != null)
                    {
                        onChunk(Chunk.Text(delta.Content));
                    }
                }

                if (json.Usage != null)
                    onChunk(Chunk.Usage(json.Usage.CompletionTokens, json.Usage.PromptTokens));
            }
        }
    }
}
```

**SanitizeToolPairing** (修复模型输出不稳定):
```csharp
public static List<Message> SanitizeToolPairing(List<Message> messages)
{
    // 1. 补全缺失的 tool_result
    // 2. 移除孤立的 tool message
    // 3. 修复截断的 JSON 参数
    // 4. 确保 tool_call/tool_result 配对
    ...
}
```

## 五、Plan Mode

**流程**:
```
用户输入 "帮我修改所有DN100管道的壁厚"
  ↓
CopilotController.SetPlanMode(true)
  ↓
AgentLoop 运行（只读工具可用，写工具被 block）
  ↓
AI 生成执行计划:
  1. 查询所有 DN100 管道 [query_elements] ✅
  2. 检查当前壁厚 [get_attributes] ✅
  3. 修改壁厚为 SCH40 [modify_attribute] 🔒 需审批
  ↓
ApprovalRequest 发送给用户
  ↓
用户审批: "全部允许"
  ↓
CopilotController.SetPlanMode(false)
  ↓
AgentLoop 继续执行修改操作
```

## 六、上下文管理

**Context Compaction** (借鉴 Reasonix):
```csharp
private async Task MaybeCompact()
{
    var ratio = (double)session.TokenCount / provider.ContextWindow;
    if (ratio > 0.8)
    {
        // 1. 识别历史消息中的关键信息
        // 2. 用 LLM 生成摘要
        // 3. 替换旧消息为摘要
        // 4. 保留最近 N 轮完整对话
        var summary = await SummarizeAsync(session.GetOldMessages());
        session.Compact(summary, keepRecent: 5);
        sink.Emit(new CopilotEvent { Kind = EventKind.Notice, Text = "Context compacted" });
    }
}
```

## 七、内置工具清单

### 查询类（14 个）

| 工具名 | 优先级 | 执行方式 | 典型场景 |
|--------|:------:|---------|---------|
| `query_elements` | P0 | PML | "查所有 DN100 管道" |
| `get_attributes` | P0 | C# 直调 | "查 PIPE-001 的壁厚" |
| `get_children` | P0 | PML | "列出 ZONE-01 下所有设备" |
| `get_hierarchy` | P0 | PML | "显示完整层级树" |
| `get_element_info` | P0 | C# 直调 | "这是什么元素" |
| `get_current_element` | P0 | C# 直调 | "当前选中元素是什么" |
| `navigate_to` | P0 | C# 直调 | "定位到 PIPE-001" |
| `query_pipes` | P0 | PML | "查所有碳钢 DN150 管道" |
| `get_pipe_branches` | P0 | PML | "PIPE-001 有几个 Branch" |
| `get_fitments` | P1 | PML | "这个 Branch 有哪些管件" |
| `query_equipment` | P1 | PML | "查所有换热器" |
| `get_nozzle_info` | P1 | PML | "PUMP-001 有几个管嘴" |
| `get_position` | P1 | C# 直调 | "这个管嘴的坐标" |
| `get_model_stats` | P1 | PML | "这个 Zone 有多少管道/设备" |

### 修改类（9 个）

| 工具名 | 优先级 | 执行方式 | 典型场景 |
|--------|:------:|---------|---------|
| `set_attribute` | P0 | C# 直调 | "把 WTHK 改成 SCH40" |
| `batch_rename` | P1 | PML | "把所有管道前缀改成 XX" |
| `modify_pipe_spec` | P1 | PML | "改成 SCH40" |
| `batch_set_attribute` | P1 | PML | "批量修改壁厚" |
| `modify_nozzle` | P1 | C# 直调 | "把管嘴移到 2m 高度" |
| `refresh_bran_spec` | P2 | PML | "刷新桥架等级" |
| `set_conntray` | P2 | PML | "连接桥架到支架" |
| `fix_virtual_conn` | P2 | PML | "修复虚拟连接引用" |
| `create_equipment` | P2 | PML | "创建虚拟设备" |

### 检查类（7 个）

| 工具名 | 优先级 | 执行方式 | 典型场景 |
|--------|:------:|---------|---------|
| `check_exists` | P0 | C# 直调 | "这个设备在模型中存在吗" |
| `check_attribute_complete` | P1 | PML | "检查缺失属性的管道" |
| `check_room_number` | P1 | PML | "检查房间号是否合规" |
| `check_name_consistency` | P2 | PML | "检查命名规范" |
| `check_distance` | P2 | PML + DLL | "检查间距是否合规" |
| `check_bore_consistency` | P2 | PML | "检查管件管径一致性" |
| `get_change_status` | P2 | PML | "检查元素修改状态" |

### 计算类（4 个）

| 工具名 | 优先级 | 执行方式 | 典型场景 |
|--------|:------:|---------|---------|
| `calculate_distance` | P1 | PML | "两个元素距离/角度/中点" |
| `get_orientation_wrt` | P1 | PML | "管嘴相对于设备的朝向" |
| `get_ppoint_info` | P1 | PML | "管道走向信息" |
| `get_route_info` | P1 | PML | "管道路线总长度" |

### 导入导出类（5 个）

| 工具名 | 优先级 | 执行方式 | 典型场景 |
|--------|:------:|---------|---------|
| `import_excel` | P1 | PML | "导入设备清单" |
| `export_to_excel` | P1 | PML | "导出管道清单" |
| `check_and_export` | P1 | PML | "检查并导出结果" |
| `merge_excel` | P2 | PML | "合并碰撞反馈" |
| `generate_report` | P2 | PML | "生成报表" |

### 系统类（2 个）

| 工具名 | 优先级 | 执行方式 | 典型场景 |
|--------|:------:|---------|---------|
| `undo` | P0 | C# 直调 | "撤销" |
| `get_session_context` | P2 | C# 直调 | "获取当前会话上下文" |

---

## 八、线程模型

**核心约束**：E3D API 要求在 E3D 主线程（UI 线程）上调用。

```
UI 线程 (E3D 主线程)
  ├── CopilotForm (WinForms UI)
  ├── E3D API 调用 (DbElement, CommandLine, WindowManager)
  └── 审批对话框

后台线程 (Task/ThreadPool)
  ├── AgentLoop.RunAsync (LLM 调用 + 循环控制)
  ├── VllmProvider.StreamAsync (HTTP 流式)
  └── Tool.ExecuteAsync → 需切换到 UI 线程调用 E3D API
```

**线程切换机制**：
```csharp
public class ThreadMarshaller
{
    private readonly SynchronizationContext uiContext;

    public ThreadMarshaller()
    {
        // 在 E3D 主线程初始化时捕获
        uiContext = SynchronizationContext.Current;
    }

    // 在 UI 线程执行（E3D API 调用必须走这里）
    public Task<T> InvokeOnUIThread<T>(Func<T> action)
    {
        var tcs = new TaskCompletionSource<T>();
        uiContext.Post(_ =>
        {
            try { tcs.SetResult(action()); }
            catch (Exception ex) { tcs.SetException(ex); }
        }, null);
        return tcs.Task;
    }

    // 在 UI 线程执行（无返回值）
    public Task InvokeOnUIThread(Action action)
    {
        var tcs = new TaskCompletionSource<object>();
        uiContext.Post(_ =>
        {
            try { action(); tcs.SetResult(null); }
            catch (Exception ex) { tcs.SetException(ex); }
        }, null);
        return tcs.Task;
    }
}
```

**工具执行的线程切换**：
```csharp
// 所有涉及 E3D API 的工具必须通过 ThreadMarshaller
public class QueryElementsTool : IE3DTool
{
    private readonly ThreadMarshaller marshaller;

    public async Task<string> ExecuteAsync(string args)
    {
        // 切换到 UI 线程执行 E3D API
        return await marshaller.InvokeOnUIThread(() =>
        {
            // 简单查询走 PML（E3D 没有静态批量查询 C# API）
            Command cmd = Command.CreateCommand("var !list coll all PIPE for $!ce");
            cmd.RunInPdms();
            return cmd.Result;
        });
    }
}
```

## 九、审批流同步机制

**问题**：AgentLoop 在后台线程运行，审批需要 UI 线程弹出对话框，用户确认后继续。

**解决方案**：`TaskCompletionSource` + `SynchronizationContext`

```csharp
public class PendingApproval
{
    public string Id { get; } = Guid.NewGuid().ToString("N");
    public IE3DTool Tool { get; set; }
    public string Args { get; set; }
    public string Description { get; set; }

    // 内部完成源
    private readonly TaskCompletionSource<ApprovalResult> tcs
        = new TaskCompletionSource<ApprovalResult>();

    // UI 线程调用：用户点击审批按钮
    public void Complete(bool allow, bool sessionPersist = false)
    {
        tcs.SetResult(new ApprovalResult
        {
            Allow = allow,
            SessionPersist = sessionPersist
        });
    }

    // 后台线程调用：阻塞等待审批结果
    public Task<ApprovalResult> WaitAsync()
    {
        return tcs.Task;
    }
}

// AgentLoop 中的审批流程
private async Task<ToolResult> ExecuteOneAsync(ToolCall call)
{
    var tool = tools.Get(call.Name);
    var approval = await policy.CheckAsync(tool, call.Args);

    if (approval == ApprovalResult.NeedApproval)
    {
        var req = new PendingApproval { Tool = tool, Args = call.Args };

        // 发送审批事件到 UI 线程（非阻塞）
        sink.Emit(new CopilotEvent
        {
            Kind = EventKind.ApprovalRequest,
            Approval = req
        });

        // 后台线程阻塞等待用户操作（不占用 UI 线程）
        var result = await req.WaitAsync();
        if (!result.Allow) return ToolResult.Rejected("User rejected");

        // 会话级持久化
        if (result.SessionPersist)
            policy.Set(call.Name, ApprovalMode.Auto);
    }

    // 切换到 UI 线程执行 E3D API
    var output = await tool.ExecuteAsync(call.Args);
    return ToolResult.Success(output);
}
```

**审批状态流转**：
```
AgentLoop (后台线程)
  → sink.Emit(ApprovalRequest)  ──── 非阻塞发送
  → await req.WaitAsync()       ──── 阻塞等待
                                      ↑
CopilotForm (UI 线程)                   |
  → PromptShelfControl.Show()    ──── 显示审批 UI
  → 用户按 1/2/3/4              ──── 用户操作
  → req.Complete(allow)          ──── 唤醒后台线程
```

---

## 十、安全模型

**三层安全**:
```
1. Tool Policy — 静态规则（per-tool auto/ask/planOnly）
2. Permission Gate — 运行时检查（敏感操作需确认）
3. Code Review — 生成 PML 脚本预览（用户审批后执行）
```

**安全分级**:

| 级别 | 工具类型 | 默认行为 | 示例 |
|------|---------|---------|------|
| **L0 只读** | 查询/读取 | 自动执行 | query_elements, get_attributes |
| **L1 导航** | 视图切换 | 自动执行 | navigate_to, get_hierarchy |
| **L2 写入** | 单个修改 | 需确认 | set_attribute, modify_pipe_spec |
| **L3 批量** | 批量修改 | 需确认 + 预览 | batch_set_attribute |
| **L4 系统** | PML/Undo | 需确认 + 预览 | execute_pml, undo |

**E3D 特殊安全考虑**:
- E3D 进程内执行，无沙箱 → 必须代码级审查
- 写操作影响整个设计模型 → 需要预览 + 确认
- 批量操作风险高 → 限制单次操作数量
- 操作可逆性 → 记录操作日志，支持撤销

---

## 十一、借鉴来源总结

### Reasonix Desktop 借鉴（Go + Wails + React）

| 设计 | 来源模块 | E3D 实现 |
|------|---------|---------|
| Tool 接口 (`Name/Schema/Execute/ReadOnly`) | `internal/tool/tool.go` | `IE3DTool` 接口 |
| Event Sink 解耦 | `internal/event/event.go` | `IEventSink` 接口 |
| Agent Run 循环（流式+并行分区） | `internal/agent/agent.go` | `AgentLoop.RunAsync()` |
| ReadOnly 工具并行执行 | `internal/agent/agent.go` → `partitionToolCalls` | `ExecuteBatchAsync()` 并行分区 |
| Plan Mode（只读规划→审批→执行） | `internal/control/controller.go` | `PlanMode` + `ApprovalManager` |
| Tool Registry（注册+Schema） | `internal/tool/tool.go` → `Registry` | `ToolRegistry` |
| SanitizeToolPairing（修复输出） | `internal/provider/provider.go` | `SanitizeToolPairing` |
| Context Compaction | `internal/agent/agent.go` → `maybeCompact` | `MaybeCompact()` |
| Prompt 分层拼接（cache-first） | `internal/config/config.go` → `DefaultSystemPrompt` | `系统提示词.md` 分层设计 |
| Skill 系统（inline+subagent） | `internal/skill/skill.go` | v2 实现 |

### Cline CLI 借鉴（TypeScript monorepo）

| 设计 | 来源模块 | E3D 实现 |
|------|---------|---------|
| Tool Policy（per-tool 审批策略） | `internal/permission/permission.go` | `ToolPolicy` |
| Tool Presets（预配置组合） | `apps/vscode/src/core/permissions/` | `ToolPresets`（观察/确认/自动） |
| VS Code 插件架构 | `apps/vscode/src/extension.ts` | `CopilotAddin`（E3D Addin） |
| WebviewProvider 模式 | `apps/vscode/src/hosts/vscode/VscodeWebviewProvider.ts` | `CopilotForm`（WinForms） |
| 消息合并管线 | `apps/vscode/webview-ui/src/components/chat/ChatView.tsx` | `ChatListBox` 合并逻辑 |
| ButtonConfig 状态机 | `apps/vscode/webview-ui/src/components/chat/` | `ApprovalBar` 按钮配置 |
| Prompt 模板变量 | `sdk/packages/shared/src/prompt/system.ts` → `{{CWD}}` | `{{CURRENT_ELEMENT}}` 动态注入 |
| 工具 Schema 外置 | `sdk/packages/shared/src/prompt/system.ts` | API `tools` 参数传入 |

### E小智 独创

| 设计 | 说明 |
|------|------|
| PML 执行引擎 | `PmlEngine` + `PmlGenerator`，PML 为主 C# 为辅 |
| PML 黄金范式 | 5 步骨架覆盖 90%+ 操作 |
| 46 个垂直工具 | 完全针对 E3D 工厂设计场景（6 核心 + 40 专用） |
| PML 语法速查 | 精简版常驻 prompt，完整版按需加载 |
| E3D 元素上下文注入 | `{{CURRENT_ELEMENT}}` 自动感知选中元素 |

